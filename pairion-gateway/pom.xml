<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.pairion</groupId>
        <artifactId>pairion-server</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>pairion-gateway</artifactId>
    <name>Pairion Gateway</name>
    <description>REST and WebSocket endpoints — Spring Boot application entry point</description>

    <dependencies>
        <dependency>
            <groupId>com.pairion</groupId>
            <artifactId>pairion-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.pairion</groupId>
            <artifactId>pairion-adapters</artifactId>
        </dependency>
        <dependency>
            <groupId>com.pairion</groupId>
            <artifactId>pairion-household</artifactId>
        </dependency>
        <dependency>
            <groupId>com.pairion</groupId>
            <artifactId>pairion-memory</artifactId>
        </dependency>
        <dependency>
            <groupId>com.pairion</groupId>
            <artifactId>pairion-skills</artifactId>
        </dependency>
        <dependency>
            <groupId>com.pairion</groupId>
            <artifactId>pairion-agent</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>8.0</version>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>${archunit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <jvmArguments>--enable-preview --enable-native-access=ALL-UNNAMED</jvmArguments>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
