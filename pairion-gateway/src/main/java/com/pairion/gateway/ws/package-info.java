/**
 * WebSocket handlers for the Pairion real-time protocol.
 *
 * <p>Handles the {@code /ws/v1} WebSocket endpoint including session lifecycle, heartbeat, and
 * binary audio frame routing.
 */
package com.pairion.gateway.ws;
