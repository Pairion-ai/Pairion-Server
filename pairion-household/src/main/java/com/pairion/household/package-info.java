/**
 * Household management module for Pairion Server.
 *
 * <p>Manages the household entity, user registry (Owner, Member, Minor, Guest), voice enrollment,
 * and role-based access enforcement. Per-user partitioning of data is a hard invariant.
 */
package com.pairion.household;
