/**
 * Memory subsystem for Pairion Server.
 *
 * <p>Manages episodic memory (conversation history) and semantic memory (extracted preferences and
 * facts) with strict per-user partitioning.
 */
package com.pairion.memory;
