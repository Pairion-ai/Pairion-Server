/**
 * Adapter layer for Pairion Server.
 *
 * <p>Every external capability is accessed through an SPI interface in this package hierarchy.
 * Vendor SDK imports are permitted only within adapter implementation subpackages. No other Pairion
 * module may import vendor SDKs directly.
 */
package com.pairion.adapters;
